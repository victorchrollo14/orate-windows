<Window x:Class="Orate.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        IsHitTestVisible="False"
        Focusable="False"
        SizeToContent="Manual"
        Width="28" Height="10">
    <Border x:Name="Pill"
            CornerRadius="5"
            Background="#80000000">
        <Canvas x:Name="PillCanvas" ClipToBounds="True" />
    </Border>
</Window>
